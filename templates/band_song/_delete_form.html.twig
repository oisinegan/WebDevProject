<form method="post" action="{{ path('app_band_song_delete', {'id': band_song.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ band_song.id) }}">
    <button class="btn">Delete</button>
</form>
