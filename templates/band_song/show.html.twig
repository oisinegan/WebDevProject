{% extends 'base.html.twig' %}

{% block title %}BandSong{% endblock %}

{% block body %}
    <h1>BandSong</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ band_song.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_band_song_index') }}">back to list</a>

    <a href="{{ path('app_band_song_edit', {'id': band_song.id}) }}">edit</a>

    {{ include('band_song/_delete_form.html.twig') }}
{% endblock %}
